# Tuleap Configuration
# Copy this file to .env and update the values

# Your Tuleap domain (use localhost for local development)
TULEAP_FQDN="localhost"

# MySQL root password (use a strong password in production)
MYSQL_ROOT_PASSWORD="your-mysql-root-password"

# Tuleap database user password (use a strong password in production)
TULEAP_SYS_DBPASSWD="your-tuleap-db-password"

# Tuleap site administrator password (use a strong password in production)
SITE_ADMINISTRATOR_PASSWORD="your-admin-password"

# Note: Use only alphanumeric characters in passwords to avoid MySQL compatibility issues